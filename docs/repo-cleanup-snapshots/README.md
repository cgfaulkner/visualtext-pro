# Repo cleanup snapshots

Pre-cleanup state captured for rollback reference.

- **pre-cleanup-summary.txt** — Inventory vs filesystem mismatches and list of screenshots to capture.
- **Screenshots:** Capture via Finder or VS Code per the list in pre-cleanup-summary.txt and save in this folder (e.g. `repo-root.png`, `core.png`, `shared.png`, and any of Documents to Review, Documents_to_Review, Slides to Review, Complete, Temp if they exist).
