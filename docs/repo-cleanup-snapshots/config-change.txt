config.yaml paths — before/after (Task B, 2026-01-27)
=====================================================

BEFORE:
  input_folder: Slides to Review
  output_folder: Reviewed Reports
  thumbnail_folder: Slide Thumbnails
  temp_folder: Temp

AFTER:
  input_folder: documents_to_review
  output_folder: reviewed_reports
  thumbnail_folder: slide_thumbnails
  temp_folder: temp

Runtime path validation: Dry-run not executed in this run (environment missing yaml). Re-run `python altgen.py --dry-run process "documents_to_review"` after documents_to_review/ exists and deps are installed; if path_validator rejects, record TODO in docs/cleanup-summary.md.
